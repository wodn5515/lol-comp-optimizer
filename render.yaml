services:
  - type: web
    name: lol-comp-optimizer-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./lol_comp.db
      - key: CORS_ORIGINS
        value: https://wodn5515.github.io,http://localhost:5173,http://localhost:3000
    plan: free
